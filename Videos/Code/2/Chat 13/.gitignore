# ── Android Build ─────────────────────────────────────────────
.gradle/
build/
app/build/
macrobenchmark/build/
*.apk
*.aab
*.ap_

# ── Local Config (NEVER commit these) ─────────────────────────
local.properties
google-services.json
app/src/dev/google-services.json
app/src/prod/google-services.json

# ── Keystore & Signing (NEVER commit these) ───────────────────
*.jks
*.keystore
keystore/

# ── IDEs ──────────────────────────────────────────────────────
.idea/
*.iml
.DS_Store
Thumbs.db

# ── Kotlin / Java ─────────────────────────────────────────────
*.class
*.jar
out/

# ── Room Schemas (optional — keep if you want schema in VCS) ──
# app/schemas/

# ── Claude Code (commit shared bits, ignore local state) ──────
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/.session_log
.claude/cache/

# ── Cloud Functions ───────────────────────────────────────────
functions/node_modules/
functions/lib/

# ── Tools ─────────────────────────────────────────────────────
tools/__pycache__/
tools/wikidata_export.csv
tools/wikidata_export.json
